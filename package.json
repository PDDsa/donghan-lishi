{
  "name": "western-han-kids-h5",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:deploy": "npm run build && wrangler deploy",
    "worker:preview": "npm run build && wrangler versions upload"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "wrangler": "^4.93.1"
  }
}
